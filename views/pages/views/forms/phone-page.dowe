page PhonePage
  signal contactPhone value:""
  const phoneProps:
    value:[
      {
        prop:"bind / value"
        type:"string Signal path / string"
        defaultValue:"—"
      },
      {
        prop:"country"
        type:"ISO 3166 alpha-2 code"
        defaultValue:"—"
      },
      {
        prop:"dialCodeName"
        type:"string"
        defaultValue:"dialCode"
      },
      {
        prop:"priorityCountries"
        type:"comma-separated country codes"
        defaultValue:"—"
      },
      {
        prop:"searchPlaceholder / emptyText / loadingText"
        type:"string"
        defaultValue:"Reference defaults"
      },
      {
        prop:"label / placeholder / labelFloating"
        type:"string / string / boolean"
        defaultValue:"— / Enter phone number / false"
      },
      {
        prop:"disabled"
        type:"boolean"
        defaultValue:"false"
      },
      {
        prop:"variant"
        type:"solid | soft | outlined | line"
        defaultValue:"outlined"
      },
      {
        prop:"scheme"
        type:"primary | secondary | tertiary | muted | success | info | warning | danger"
        defaultValue:"primary"
      },
      {
        prop:"size"
        type:"sm | md | lg"
        defaultValue:"md"
      },
      {
        prop:"name / helpText / errorText"
        type:"string"
        defaultValue:"—"
      },
      {
        prop:"validate"
        type:"direct child"
        defaultValue:"—"
      },
    ]
  Section id:"phone-introduction" py:{ xs:6 md:10 }
    Grid columns:1 gap:5
      Grid columns:1 gap:3
        Text size:"xs" weight:"bold" spacing:"widest" color:"muted"
          "FORM"
        Title size:{ xs:"3xl" md:"6xl" } weight:"black"
          "Phone"
        Text size:"lg" color:"muted"
          "Phone pairs a searchable Dowe-owned country catalog with digit-only local-number binding and explicit dial-code form storage. It uses the same inset, anchored popover, compact search, and horizontal country rows on every target."
  Section id:"phone-playground" p:{ xs:5 md:7 }
    Grid columns:1 gap:6
      Grid columns:1 gap:2
        Text size:"xs" weight:"bold" spacing:"widest" color:"muted"
          "PLAYGROUND"
        Title size:"3xl" weight:"black"
          "Collect an international contact number"
        Text size:"sm" color:"muted"
          "Search the Dowe country popover, keep Colombia and nearby markets at the top, and store the selected dial code separately from the digit-only bound local number."
      Card variant:"outlined" p:{ xs:5 md:7 } border:1
        Grid columns:1 gap:5
          Phone:
            bind:contactPhone
            country:"CO"
            dialCodeName:"contactDialCode"
            priorityCountries:"CO,US,MX,BR,ES"
            label:"Mobile phone"
            placeholder:"300 123 4567"
            searchPlaceholder:"Search country or dial code"
            emptyText:"No country matches this search"
            loadingText:"Loading countries"
            helpText:"Choose the country first, then enter digits for the local number."
            variant:"outlined"
            scheme:"primary"
            size:"lg"
            name:"mobile-phone"
            validate rule:"required" message:"Enter a mobile phone number."
            validate rule:"phone" message:"Enter a valid phone number."
          Grid columns:1 gap:4
            Text size:"sm" color:"muted"
              "bind stores only local-number digits so applications can validate and format them independently."
            Text size:"sm" color:"muted"
              "dialCodeName gives the generated form a stable companion field for the selected calling code."
      Code:
        language:"dowe"
        variant:"soft"
        scheme:"surface"
        copyLabel:"Copy Dowe"
        copiedLabel:"Copied"
        content:"""
          signal contactPhone value:""

          Phone bind:contactPhone country:"CO" dialCodeName:"contactDialCode" priorityCountries:"CO,US,MX,BR,ES" label:"Mobile phone" placeholder:"300 123 4567" searchPlaceholder:"Search country or dial code" emptyText:"No country matches this search" loadingText:"Loading countries" helpText:"Choose the country first, then enter digits for the local number." variant:"outlined" scheme:"primary" size:"lg" name:"mobile-phone"
            validate rule:"required" message:"Enter a mobile phone number."
            validate rule:"phone" message:"Enter a valid phone number."
        """
  Section id:"phone-regions" py:{ xs:7 md:10 }
    Grid columns:1 gap:6
      Grid columns:1 gap:2
        Text size:"xs" weight:"bold" spacing:"widest" color:"muted"
          "REGIONAL WORKFLOWS"
        Title size:"3xl" weight:"black"
          "Prioritize the countries that matter"
        Text size:"sm" color:"muted"
          "The selected country appears first, followed by priorityCountries and then the rest of the built-in catalog."
      Grid columns:1 gap:5
        Phone:
          value:"4155550136"
          country:"US"
          dialCodeName:"billingDialCode"
          priorityCountries:"US,CA,MX"
          label:"Billing contact"
          placeholder:"415 555 0136"
          helpText:"North American markets stay at the top of the selector."
          variant:"soft"
          scheme:"secondary"
          size:"md"
          name:"billing-phone"
        Phone:
          country:"ES"
          dialCodeName:"supportDialCode"
          priorityCountries:"ES,GB,DE"
          label:"Support callback"
          placeholder:"612 34 56 78"
          searchPlaceholder:"Find a support region"
          helpText:"Regional support teams can start from a focused catalog."
          variant:"line"
          scheme:"success"
          size:"md"
          name:"support-phone"
      Code:
        language:"dowe"
        variant:"soft"
        scheme:"surface"
        copyLabel:"Copy Dowe"
        copiedLabel:"Copied"
        content:"""
          Phone value:"4155550136" country:"US" dialCodeName:"billingDialCode" priorityCountries:"US,CA,MX" label:"Billing contact" variant:"soft" scheme:"secondary" size:"md"

          Phone country:"ES" dialCodeName:"supportDialCode" priorityCountries:"ES,GB,DE" label:"Support callback" variant:"line" scheme:"success" size:"md"
        """
  Section id:"phone-states" p:{ xs:5 md:7 }
    Grid columns:1 gap:6
      Grid columns:1 gap:2
        Text size:"xs" weight:"bold" spacing:"widest" color:"muted"
          "STATES AND DENSITY"
        Title size:"3xl" weight:"black"
          "Keep validation attached to the whole number"
        Text size:"sm" color:"muted"
          "Error feedback belongs to the combined country and number control, while disabled preserves both values visibly."
      Grid columns:1 gap:5
        Phone:
          value:"123"
          country:"MX"
          label:"Emergency contact"
          errorText:"Enter a complete local number for Mexico."
          variant:"outlined"
          scheme:"danger"
          size:"md"
          name:"emergency-phone"
        Phone:
          value:"2079460128"
          country:"GB"
          label:"Verified phone"
          disabled:true
          helpText:"Remove phone verification before changing this number."
          variant:"solid"
          scheme:"muted"
          size:"md"
          name:"verified-phone"
      Text size:"sm" color:"muted"
        "The base heights are 32, 40, and 48; labelFloating adds 8 logical units without changing the country trigger."
      Text size:"sm" color:"muted"
        "Phone validates the bound local-number string after blur or country-popover close; the first failing message belongs to the combined control."
      Flex wrap:true justify:"center" align:"center" gap:4
        Phone country:"CO" label:"Small" labelFloating:true variant:"soft" scheme:"primary" size:"sm"
        Phone country:"CO" label:"Medium" labelFloating:true variant:"soft" scheme:"primary" size:"md"
        Phone country:"CO" label:"Large" labelFloating:true variant:"soft" scheme:"primary" size:"lg"
  Section id:"phone-contract" py:{ xs:7 md:10 }
    Grid columns:1 gap:5
      Grid columns:1 gap:2
        Text size:"xs" weight:"bold" spacing:"widest" color:"muted"
          "BEHAVIOR"
        Title size:"3xl" weight:"black"
          "One country catalog, portable selection"
        Text size:"sm" color:"muted"
          "Dowe owns country metadata, ordering, search, dial-code storage, selected state, and the shared circle-flags SVGs across generated targets."
      Grid columns:1 gap:4
        Card variant:"soft" p:5
          Grid columns:1 gap:2
            Text size:"xs" weight:"bold" color:"muted"
              "CATALOG"
            Title size:"lg" weight:"bold"
              "Searchable countries"
            Text size:"sm" color:"muted"
              "Country names, flags, and dial codes come from the deterministic Dowe-owned catalog."
        Card variant:"soft" p:5
          Grid columns:1 gap:2
            Text size:"xs" weight:"bold" color:"muted"
              "VALUE"
            Title size:"lg" weight:"bold"
              "Digit-only local value"
            Text size:"sm" color:"muted"
              "bind owns local-number digits and dialCodeName identifies the generated companion form value."
        Card variant:"soft" p:5
          Grid columns:1 gap:2
            Text size:"xs" weight:"bold" color:"muted"
              "TARGETS"
            Title size:"lg" weight:"bold"
              "Dowe popovers"
            Text size:"sm" color:"muted"
              "Web, SwiftUI, Compose, and the Android launcher use the Dowe anchored dropdown surface with matching inset, width, compact search, horizontal flag/name/dial rows, empty feedback, selection, and vector country flags."
  Section id:"phone-props" py:{ xs:7 md:10 }
    Grid columns:1 gap:5
      Grid columns:1 gap:2
        Text size:"xs" weight:"bold" spacing:"widest" color:"muted"
          "API"
        Title size:"3xl" weight:"black"
          "Props"
        Text size:"sm" color:"muted"
          "Phone also accepts the applicable common spacing, sizing, visibility, accessibility, and shadow props."
      Table:
        data:phoneProps
        variant:"soft"
        scheme:"surface"
        size:"sm"
        striped:true
        dividers:true
        rounded:"md"
        column field:"prop" label:"Prop" width:"1fr"
        column field:"type" label:"Type" width:"3fr"
        column field:"defaultValue" label:"Default" width:"1fr"
